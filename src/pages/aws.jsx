import React from 'react';
import Layout from '@Modules/layout';
import Table from '@Modules/table';

const Aws = () => {
  return (
    <Layout>
      <Table />
    </Layout>
  )
}

export default Aws;
